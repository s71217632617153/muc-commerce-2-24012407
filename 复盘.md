# Day 9 复盘 (100-200字)

今天的实验让我第一次真正理解了机器学习的工作流程。通过6人小实验，我发现人工规则（满意度低+投诉+短tenure）只能正确判断3/6个用户，说明靠经验设阈值不可靠。

数据验收确认了5630行22列无缺失，流失率16.84%。建模口径中CustomerID和Churn必须排除出特征矩阵——前者是无信息的唯一标识，后者是要预测的答案。特征方案自动区分了14个数值列和6个类别列，文字类别必须用OneHot编码转为0/1，否则模型无法计算。

分层划分（stratify=y）让训练集和测试集的流失比例都接近16.84%，避免了随机划分导致少数类分布不均的问题。预处理流水线将20个特征转换为36列（14标准化数值+22个OneHot列），全部为数值、无缺失、无无穷值。

最有启发性的是最低参照线：永远预测"未流失"就能拿到83.13%准确率，但流失召回率为0——一个流失用户都没找到。这说明在类别不平衡的场景下，准确率是误导性指标，必须关注召回率、精确率等针对少数类的指标。这也是后续模型要超越的最低门槛。
